
/*
 *  laplacian.h
 */

#ifndef _DELAUNAY_H_
#define _DELAUNAY_H_

#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <string.h>


/* //////////////////////////// Les STRUCTURES ///////////////////////////////////// */
typedef struct Node Node;
struct Node {
int nChild; 
int indice;
struct Node *voisins;
}; 

typedef struct Edge Edge;
struct Edge {
int indice; 
int weight;
int f; // le flot ? on pourra surement se passer de stocker cela là. Voir vecteur de section 5 
Node *a;
Node *b;
};


#endif
